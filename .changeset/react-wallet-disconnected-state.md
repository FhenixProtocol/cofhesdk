---
'@cofhe/react': minor
---

When no wallet is connected, the portal now hides the wallet header and navigation and shows a message asking the user to connect.

Add `react.projectName` so apps can include their name in that message.
