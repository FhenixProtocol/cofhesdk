{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["HOME", "USERPROFILE", "VOCS_BASE_URL"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"],
      "dependsOn": ["^build"],
      "env": ["HOME", "USERPROFILE", "VOCS_BASE_URL"]
    },
    "@cofhe/test-setup#build": {
      "cache": false,
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "passThroughEnv": ["TEST_PRIVATE_KEY", "TEST_LOCALCOFHE_PRIVATE_KEY", "PRIMARY_TEST_CHAIN"]
    },
    "@cofhe/site#build": {
      "cache": false,
      "dependsOn": ["^build"],
      "env": ["HOME", "USERPROFILE", "VOCS_BASE_URL"]
    },
    "test": {
      "dependsOn": ["^build"],
      "passThroughEnv": ["TEST_PRIVATE_KEY", "CI"]
    },
    "lint": {
      "env": ["HOME", "USERPROFILE", "VOCS_BASE_URL"]
    },
    "check:types": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["HOME", "USERPROFILE", "VOCS_BASE_URL"]
    },
    "clean": {
      "cache": false
    }
  }
}
