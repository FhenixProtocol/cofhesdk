{
	"private": true,
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev",
		"test": "turbo run test",
		"lint": "turbo run lint",
		"clean": "turbo run clean && rm -rf node_modules",
		"test": "turbo run test",
		"coverage": "turbo run coverage",
		"format": "prettier --write \"**/*.{ts,tsx,md}\"",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"release": "turbo run build --filter=@cofhesdk/docs^... && changeset publish"
	},
	"devDependencies": {
		"@changesets/cli": "^2.27.1",
		"eslint": "^8.57.0",
		"prettier": "^3.2.5",
		"turbo": "^2.5.6",
		"vitest": "^1.6.0",
		"@vitest/coverage-v8": "^1.6.0"
	},
	"packageManager": "pnpm@8.15.6",
	"engines": {
		"node": ">=18"
	},
	"name": "with-changesets"
}
