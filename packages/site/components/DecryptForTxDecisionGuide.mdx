<a
   href="#"
   aria-label="Open decision guide in popup"
   style={{ display: 'block', textDecoration: 'none', cursor: 'pointer' }}
   onClick={(e) => {
      e.preventDefault();
      const dialog = document.getElementById('decision-guide-dialog');
      if (dialog instanceof HTMLDialogElement) dialog.showModal();
   }}
>
   <div style={{ maxWidth: 300, margin: '0 auto', overflowX: 'auto' }}>

```mermaid
%%{init: { "flowchart": { "nodeSpacing": 20, "rankSpacing": 25 } }}%%
flowchart TD
   A[ACL policy for this ctHash] --> B{Allows anyone to decrypt?}

   B -- Yes --> C[No permit]
   C --> D["decryptForTx(ctHash)<br/>.withoutPermit()<br/>.execute()"]

   B -- No --> E[Permit required]
   E --> F{Who is the allowed decryptor?}

   F -- Your address --> G[Generate a self-permit]
   G --> H["decryptForTx(ctHash)<br/>.withPermit()<br/>.execute()"]

   F -- Different address --> I[Import a permit from them]
   I --> J["decryptForTx(ctHash)<br/>.withPermit(importedPermit)<br/>.execute()"]
```

   </div>
</a>

<dialog
   id="decision-guide-dialog"
   style={{
      width: 'min(1100px, 96vw)',
      maxHeight: '90vh',
      overflow: 'auto',
      padding: 16,
      textAlign: 'center',
   }}
   onClick={(e) => {
      const currentTarget = e.currentTarget;
      if (e.target === currentTarget && currentTarget instanceof HTMLDialogElement) {
         currentTarget.close();
      }
   }}
>
   <div style={{ overflow: 'auto' }}>

```mermaid
%%{init: { "flowchart": { "nodeSpacing": 20, "rankSpacing": 25 } }}%%
flowchart TD
    A[ACL policy for this ctHash] --> B{Allows anyone to decrypt?}

    B -- Yes --> C[No permit]
    C --> D["decryptForTx(ctHash)<br/>.withoutPermit()<br/>.execute()"]

    B -- No --> E[Permit required]
    E --> F{Who is the allowed decryptor?}

    F -- Your address --> G[Generate a self-permit]
    G --> H["decryptForTx(ctHash)<br/>.withPermit()<br/>.execute()"]

    F -- Different address --> I[Import a permit from them]
    I --> J["decryptForTx(ctHash)<br/>.withPermit(importedPermit)<br/>.execute()"]
```

   </div>
</dialog>

<style>{`
   #decision-guide-dialog pre {
      margin: 0 auto;
      display: table;
   }
   #decision-guide-dialog .mermaid {
      margin: 0 auto;
      display: table;
   }
   #decision-guide-dialog img {
      display: block;
      margin: 0 auto;
      max-width: 100%;
      height: auto;
   }
   #decision-guide-dialog svg {
      margin: 0 auto;
      max-width: 100%;
      height: auto;
      display: block;
   }
`}</style>
